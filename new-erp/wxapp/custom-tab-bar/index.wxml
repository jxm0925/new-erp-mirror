<view class="tabbar">
  <view class="tab-item {{active===index?'active':''}}" wx:for="{{list}}" wx:key="value" data-index="{{index}}" bindtap="select">
    <van-icon name="{{item.icon}}" size="44rpx" color="{{active===index?'#d81e06':'#858991'}}" />
    <text>{{item.text}}</text>
  </view>
</view>
